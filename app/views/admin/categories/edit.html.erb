<h2>ジャンル編集画面</h2>

<%= form_for(@category, url: admin_category_path) do |f| %>
	<soan>ジャンル</soan>>
	<%= f.text_field :name %>
	<%= f.radio_button :status, "有効" %>
	<%= f.label :status, "有効" %>
	<%= f.radio_button :status, "無効" %>
	<%= f.label :status, "無効" %>
	<%= f.submit "変更を保存する" %>
<% end %>
